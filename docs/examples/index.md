# Examples

These examples are meant to be practical starting points, not exhaustive coverage of every field.

They complement the manifests in `config/samples/` by grouping them into common operator workflows.

Start here if you want:

- a connection object and a first project
- a robot account and managed secret output
- singleton API examples such as configuration or schedules
