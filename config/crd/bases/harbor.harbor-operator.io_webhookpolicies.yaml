---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: webhookpolicies.harbor.harbor-operator.io
spec:
  group: harbor.harbor-operator.io
  names:
    categories:
    - harbor
    kind: WebhookPolicy
    listKind: WebhookPolicyList
    plural: webhookpolicies
    singular: webhookpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.projectRef.name
      name: Project
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Message
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WebhookPolicy is the Schema for the webhookpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WebhookPolicySpec defines the desired state of WebhookPolicy.
            properties:
              allowTakeover:
                description: |-
                  AllowTakeover indicates whether the operator is allowed to adopt an
                  existing webhook policy in Harbor with the same name.
                type: boolean
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy controls what happens when the Kubernetes object is deleted.
                  Delete removes the corresponding Harbor resource before removing the finalizer.
                  Orphan skips Harbor-side deletion and removes the finalizer so the
                  Kubernetes object can be deleted while leaving the Harbor resource in place.
                enum:
                - Delete
                - Orphan
                type: string
              description:
                description: Description is an optional policy description.
                type: string
              driftDetectionInterval:
                description: |-
                  DriftDetectionInterval is the interval at which the operator will check
                  for drift. A value of 0 (or omitted) disables periodic drift detection.
                type: string
              enabled:
                default: true
                description: Enabled indicates whether the policy is enabled.
                type: boolean
              eventTypes:
                description: EventTypes lists the webhook event types.
                items:
                  type: string
                minItems: 1
                type: array
              harborConnectionRef:
                description: HarborConnectionRef references the Harbor connection
                  object to use.
                properties:
                  kind:
                    default: HarborConnection
                    description: |-
                      Kind selects the Harbor connection object kind.
                      Defaults to HarborConnection.
                    enum:
                    - HarborConnection
                    - ClusterHarborConnection
                    type: string
                  name:
                    description: Name of the referenced Harbor connection object.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              name:
                description: |-
                  Name is the webhook policy name.
                  Defaults to metadata.name when omitted.
                type: string
              projectNameOrID:
                description: ProjectNameOrID is the Harbor project name or numeric
                  ID.
                type: string
              projectRef:
                description: ProjectRef references a Project CR to derive the Harbor
                  project ID.
                properties:
                  name:
                    description: Name of the Project resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Project resource. Defaults to the
                      referencing resource namespace.
                    type: string
                required:
                - name
                type: object
              reconcileNonce:
                description: ReconcileNonce forces an immediate reconcile when updated.
                type: string
              targets:
                description: Targets lists the webhook targets.
                items:
                  description: WebhookTargetSpec defines a single webhook target.
                  properties:
                    address:
                      description: Address is the webhook target address.
                      type: string
                    authHeader:
                      description: AuthHeader is the auth header to send to the webhook
                        target.
                      type: string
                    authHeaderSecretRef:
                      description: AuthHeaderSecretRef references a secret value holding
                        the auth header.
                      properties:
                        key:
                          description: |-
                            Key inside the Secret data. When omitted, the controller using this
                            reference will apply a sensible default.
                          type: string
                        name:
                          description: Name of the Secret.
                          minLength: 1
                          type: string
                        namespace:
                          description: Namespace of the Secret. Omit to use the HarborConnection
                            namespace.
                          type: string
                      required:
                      - name
                      type: object
                    payloadFormat:
                      description: PayloadFormat is the payload format (e.g. CloudEvents).
                      type: string
                    skipCertVerify:
                      description: SkipCertVerify indicates whether to skip TLS certificate
                        verification.
                      type: boolean
                    type:
                      description: Type defines the webhook notify type.
                      type: string
                  type: object
                  x-kubernetes-validations:
                  - message: authHeader and authHeaderSecretRef are mutually exclusive
                    rule: '!(has(self.authHeaderSecretRef) && size(self.authHeader)
                      > 0)'
                minItems: 1
                type: array
            required:
            - eventTypes
            - harborConnectionRef
            - targets
            type: object
            x-kubernetes-validations:
            - message: exactly one of projectRef or projectNameOrID must be set
              rule: has(self.projectRef) != has(self.projectNameOrID)
          status:
            description: WebhookPolicyStatus defines the observed state of WebhookPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              harborWebhookPolicyID:
                description: HarborWebhookPolicyID is the ID of the webhook policy
                  in Harbor.
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              targetsHash:
                description: TargetsHash is a hash of the configured targets to detect
                  changes.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
