name: Verify Chart CRDs

on:
  pull_request:
    paths:
      - config/crd/bases/**
      - charts/harbor-operator/crds/**
      - config/rbac/role.yaml
      - charts/harbor-operator/templates/clusterrole.yaml
      - hack/sync-chart-crds.sh
      - hack/sync-chart-rbac.sh
  push:
    branches:
      - main
    paths:
      - config/crd/bases/**
      - charts/harbor-operator/crds/**
      - config/rbac/role.yaml
      - charts/harbor-operator/templates/clusterrole.yaml
      - hack/sync-chart-crds.sh
      - hack/sync-chart-rbac.sh

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.1
      - name: Sync chart CRDs
        run: ./hack/sync-chart-crds.sh
      - name: Sync chart RBAC
        run: ./hack/sync-chart-rbac.sh
      - name: Verify no diff
        run: git diff --exit-code
