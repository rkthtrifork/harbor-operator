---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterharborconnections.harbor.harbor-operator.io
spec:
  group: harbor.harbor-operator.io
  names:
    categories:
    - harbor
    kind: ClusterHarborConnection
    listKind: ClusterHarborConnectionList
    plural: clusterharborconnections
    singular: clusterharborconnection
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.baseURL
      name: BaseURL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Message
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterHarborConnection is the Schema for the clusterharborconnections
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HarborConnectionSpec defines the desired state of HarborConnection.
            properties:
              baseURL:
                description: BaseURL is the Harbor API endpoint.
                format: url
                type: string
              caBundle:
                description: CABundle is a PEM-encoded CA bundle for validating Harbor
                  TLS certificates.
                type: string
              caBundleSecretRef:
                description: |-
                  CABundleSecretRef references a Secret containing a PEM-encoded CA bundle.
                  When set, it is mutually exclusive with caBundle.
                properties:
                  key:
                    description: |-
                      Key inside the Secret data. When omitted, the controller using this
                      reference will apply a sensible default.
                    type: string
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret. Omit to use the HarborConnection
                      namespace.
                    type: string
                required:
                - name
                type: object
              credentials:
                description: Credentials holds the default credentials for Harbor
                  API calls.
                properties:
                  passwordSecretRef:
                    description: PasswordSecretRef points to the Kubernetes Secret
                      that stores the password / token.
                    properties:
                      key:
                        description: |-
                          Key inside the Secret data. When omitted, the controller using this
                          reference will apply a sensible default.
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. Omit to use the HarborConnection
                          namespace.
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    default: basic
                    description: Type of the credential, e.g., "basic".
                    enum:
                    - basic
                    type: string
                  username:
                    description: Username for authentication.
                    minLength: 1
                    type: string
                required:
                - passwordSecretRef
                - type
                - username
                type: object
            required:
            - baseURL
            type: object
            x-kubernetes-validations:
            - message: caBundle and caBundleSecretRef are mutually exclusive
              rule: '!(has(self.caBundleSecretRef) && size(self.caBundle) > 0)'
          status:
            description: HarborConnectionStatus defines the observed state of HarborConnection.
            properties:
              authenticated:
                description: Authenticated indicates whether the connection was successfully
                  authenticated.
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
