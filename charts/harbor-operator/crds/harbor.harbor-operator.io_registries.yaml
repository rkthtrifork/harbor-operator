---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: registries.harbor.harbor-operator.io
spec:
  group: harbor.harbor-operator.io
  names:
    categories:
    - harbor
    kind: Registry
    listKind: RegistryList
    plural: registries
    singular: registry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Message
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Registry is the Schema for the registries API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RegistrySpec defines the desired state of Registry.
            properties:
              allowTakeover:
                description: |-
                  AllowTakeover indicates whether the operator is allowed to adopt an
                  existing registry in Harbor with the same name.
                type: boolean
              caCertificate:
                description: CACertificate is the PEM-encoded CA certificate for this
                  registry endpoint.
                type: string
              caCertificateRef:
                description: |-
                  CACertificateRef references a secret value holding the PEM-encoded CA certificate.
                  If set, it overrides CACertificate.
                properties:
                  key:
                    description: |-
                      Key inside the Secret data. When omitted, the controller using this
                      reference will apply a sensible default.
                    type: string
                  name:
                    description: Name of the Secret.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace of the Secret. Omit to use the HarborConnection
                      namespace.
                    type: string
                required:
                - name
                type: object
              credential:
                description: Credential holds authentication details for the registry.
                properties:
                  accessKeySecretRef:
                    description: AccessKeySecretRef references the secret key holding
                      the access key (username).
                    properties:
                      key:
                        description: |-
                          Key inside the Secret data. When omitted, the controller using this
                          reference will apply a sensible default.
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. Omit to use the HarborConnection
                          namespace.
                        type: string
                    required:
                    - name
                    type: object
                  accessSecretSecretRef:
                    description: AccessSecretSecretRef references the secret key holding
                      the access secret (password/token).
                    properties:
                      key:
                        description: |-
                          Key inside the Secret data. When omitted, the controller using this
                          reference will apply a sensible default.
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Secret. Omit to use the HarborConnection
                          namespace.
                        type: string
                    required:
                    - name
                    type: object
                  type:
                    description: Type of the credential, e.g. "basic" or "oauth".
                    enum:
                    - basic
                    - oauth
                    type: string
                required:
                - accessKeySecretRef
                - accessSecretSecretRef
                - type
                type: object
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy controls what happens when the Kubernetes object is deleted.
                  Delete removes the corresponding Harbor resource before removing the finalizer.
                  Orphan skips Harbor-side deletion and removes the finalizer so the
                  Kubernetes object can be deleted while leaving the Harbor resource in place.
                enum:
                - Delete
                - Orphan
                type: string
              description:
                description: Description is an optional description.
                type: string
              driftDetectionInterval:
                description: |-
                  DriftDetectionInterval is the interval at which the operator will check
                  for drift. A value of 0 (or omitted) disables periodic drift detection.
                type: string
              harborConnectionRef:
                description: |-
                  HarborConnectionRef references the Harbor connection object to use.
                  When the operator is started with --harbor-connection, this field may be omitted.
                properties:
                  kind:
                    description: |-
                      Kind selects the Harbor connection object kind.
                      When omitted, controllers treat it as HarborConnection.
                    enum:
                    - HarborConnection
                    - ClusterHarborConnection
                    type: string
                  name:
                    description: Name of the referenced Harbor connection object.
                    type: string
                required:
                - name
                type: object
              insecure:
                description: Insecure indicates if remote certificates should be verified.
                type: boolean
              reconcileNonce:
                description: ReconcileNonce forces an immediate reconcile when updated.
                type: string
              type:
                description: Type of the registry, e.g., "github-ghcr".
                enum:
                - github-ghcr
                - ali-acr
                - aws-ecr
                - azure-acr
                - docker-hub
                - docker-registry
                - google-gcr
                - harbor
                - huawei-SWR
                - jfrog-artifactory
                - tencent-tcr
                - volcengine-cr
                type: string
              url:
                description: URL is the registry URL.
                format: url
                type: string
            required:
            - insecure
            - type
            - url
            type: object
            x-kubernetes-validations:
            - message: caCertificate and caCertificateRef are mutually exclusive
              rule: '!(has(self.caCertificateRef) && size(self.caCertificate) > 0)'
          status:
            description: RegistryStatus defines the observed state of Registry.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              credentialHash:
                description: CredentialHash is a hash of the configured credential
                  to detect changes.
                type: string
              harborRegistryID:
                description: HarborRegistryID is the ID of the registry in Harbor.
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
