---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: replicationpolicies.harbor.harbor-operator.io
spec:
  group: harbor.harbor-operator.io
  names:
    kind: ReplicationPolicy
    listKind: ReplicationPolicyList
    plural: replicationpolicies
    singular: replicationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceRegistryRef.name
      name: Source
      type: string
    - jsonPath: .spec.destinationRegistryRef.name
      name: Destination
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Message
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ReplicationPolicy is the Schema for the replicationpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReplicationPolicySpec defines the desired state of ReplicationPolicy.
            properties:
              allowTakeover:
                description: |-
                  AllowTakeover indicates whether the operator is allowed to adopt an
                  existing replication policy in Harbor with the same name.
                type: boolean
              copyByChunk:
                description: CopyByChunk indicates whether to enable copy by chunk.
                type: boolean
              description:
                description: Description is an optional policy description.
                type: string
              destNamespace:
                description: DestNamespace is the destination namespace.
                type: string
              destNamespaceReplaceCount:
                description: DestNamespaceReplaceCount controls namespace replacement
                  behavior.
                type: integer
              destinationRegistryID:
                description: DestinationRegistryID sets the destination registry by
                  Harbor registry ID.
                type: integer
              destinationRegistryRef:
                description: DestinationRegistryRef references a Registry CR to use
                  as the destination.
                properties:
                  name:
                    description: Name of the Registry resource.
                    type: string
                  namespace:
                    description: Namespace of the Registry resource. Defaults to the
                      referencing resource namespace.
                    type: string
                required:
                - name
                type: object
              driftDetectionInterval:
                description: |-
                  DriftDetectionInterval is the interval at which the operator will check
                  for drift. A value of 0 (or omitted) disables periodic drift detection.
                type: string
              enabled:
                description: Enabled indicates whether the policy is enabled.
                type: boolean
              filters:
                description: Filters defines the replication filters.
                items:
                  description: ReplicationFilterSpec defines a replication filter.
                  properties:
                    decoration:
                      description: Decoration defines how to interpret the filter.
                      type: string
                    type:
                      description: Type defines the filter type.
                      type: string
                    value:
                      description: Value defines the filter value.
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type: array
              harborConnectionRef:
                description: HarborConnectionRef references the HarborConnection resource
                  to use.
                type: string
              name:
                description: |-
                  Name is the policy name.
                  Defaults to metadata.name when omitted.
                type: string
              override:
                description: Override indicates whether to overwrite destination resources.
                type: boolean
              reconcileNonce:
                description: ReconcileNonce forces an immediate reconcile when updated.
                type: string
              replicateDeletion:
                description: ReplicateDeletion indicates whether delete operations
                  are replicated.
                type: boolean
              singleActiveReplication:
                description: SingleActiveReplication avoids overlapping executions.
                type: boolean
              sourceRegistryID:
                description: SourceRegistryID sets the source registry by Harbor registry
                  ID.
                type: integer
              sourceRegistryRef:
                description: SourceRegistryRef references a Registry CR to use as
                  the source.
                properties:
                  name:
                    description: Name of the Registry resource.
                    type: string
                  namespace:
                    description: Namespace of the Registry resource. Defaults to the
                      referencing resource namespace.
                    type: string
                required:
                - name
                type: object
              speed:
                description: Speed is the speed limit for each task.
                type: integer
              trigger:
                description: Trigger defines when the replication policy runs.
                properties:
                  settings:
                    description: Settings holds trigger settings.
                    properties:
                      cron:
                        description: Cron is the cron expression for scheduled triggers.
                        type: string
                    type: object
                  type:
                    description: Type defines the trigger type (manual, event_based,
                      scheduled).
                    type: string
                type: object
            required:
            - harborConnectionRef
            type: object
          status:
            description: ReplicationPolicyStatus defines the observed state of ReplicationPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              harborReplicationPolicyID:
                description: HarborReplicationPolicyID is the ID of the policy in
                  Harbor.
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
